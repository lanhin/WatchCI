# Example project — copy and edit. Disabled unless REPO_URL is a real remote.

NAME=example
PROVIDER=github
REPO_URL=https://github.com/example/repo.git
OWNER=example
REPO=repo
BRANCHES=main
WATCH_PRS=true
PR_LABELS=
SCRIPT=./scripts/ci.example.sh
WORKDIR=
TIMEOUT_SEC=600
TOKEN_ENV=GITHUB_TOKEN
CLONE_DIR=
ENABLED=false
